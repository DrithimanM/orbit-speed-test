{
  "name": "orbit-speed-test",
  "version": "1.0.0",
  "private": true,
  "description": "A static, privacy-conscious network speed test",
  "scripts": {
    "test": "node --test tests/*.test.cjs && node tests/integration.cjs",
    "check": "node --check dist/security.js && node --check dist/core.js && node --check dist/app.js && node --check scripts/serve.mjs",
    "start": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
